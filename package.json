{
  "name": "gardens-metrics",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ 0xGabi/gardens-metrics",
    "create-local": "graph create --node http://localhost:8020/ 0xGabi/gardens-metrics",
    "remove-local": "graph remove --node http://localhost:8020/ 0xGabi/gardens-metrics",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 0xGabi/gardens-metrics"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
